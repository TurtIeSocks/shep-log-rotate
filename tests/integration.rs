//! Placeholder so the `[[test]]` target in `Cargo.toml` (required from Task 1
//! onward, since it names this path) has a file to point at. A later task
//! replaces this with the real, `integration`-feature-gated test suite.
